# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/perpustakaan?ssl=false
spring.datasource.username=postgres
spring.datasource.password=anwarjuni123
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pooling & Validation
spring.datasource.testOnBorrow=true
spring.datasource.validationQuery=SELECT 1
spring.datasource.validationInterval=10000

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Jackson Config
spring.jackson.serialization.fail-on-empty-beans=false

# Email Configuration (SMTP Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.username=muthmauleaaa@gmail.com
spring.mail.password=dkeh xzwz tiwb ucpt
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.smtp.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload Configuration
file.upload-dir=uploads/
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=100MB
spring.servlet.multipart.enabled=true